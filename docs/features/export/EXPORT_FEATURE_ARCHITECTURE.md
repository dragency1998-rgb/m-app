# Export Feature - Visual Architecture & Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    ReportsModule Component                       │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Filter Section                                             │ │
│  │ ├─ Due Date Filters                                        │ │
│  │ ├─ Status Filters                                          │ │
│  │ ├─ Payment Type Filters                                    │ │
│  │ ├─ Buyer/Mfg Selection                                     │ │
│  │ └─ Action Buttons:                                         │ │
│  │    ├─ [Apply Filters] ← NOW: Just applies filters         │ │
│  │    │                    (OLD: Triggered CSV export)        │ │
│  │    └─ [Clear Filters]                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Summary Statistics Cards                                   │ │
│  │ ├─ Total Invoices / Orders                                │ │
│  │ ├─ Total Amount / Quantity                                │ │
│  │ ├─ Overdue / Due Soon (invoices)                          │ │
│  │ └─ More analytics...                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Detailed Report Section Header                            │ │
│  │                                                            │ │
│  │  "Detailed Report"        [Export / Download] ← NEW       │ │
│  │                          (Green button)                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Detailed Report Cards / Table                              │ │
│  │ ├─ Buyer-wise Report                                      │ │
│  │ ├─ Manufacturer-wise Report                               │ │
│  │ └─ Invoice Details Table (if in details view)             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

                              │
                              ▼
        ┌─────────────────────────────────────┐
        │   ExportModal Component (NEW)       │ ◄─── Renders on demand
        │                                     │      when Export clicked
        │  ┌─────────────────────────────────┐│
        │  │ Export Report              [X]  ││
        │  ├─────────────────────────────────┤│
        │  │                                 ││
        │  │ 📄 Download as CSV             ││
        │  │    (lightweight format)        ││
        │  │                                 ││
        │  │ 📄 Download as PDF             ││
        │  │    (professional format)       ││
        │  │                                 ││
        │  │         [Cancel]                ││
        │  └─────────────────────────────────┘│
        └─────────────────────────────────────┘
              │                  │
              ▼                  ▼
    ┌──────────────────┐  ┌──────────────────┐
    │ CSV Export Flow  │  │ PDF Export Flow  │
    └──────────────────┘  └──────────────────┘
```

---

## Data Flow Diagram

```
USER INTERACTION
    │
    ├─────────────────────┬──────────────────┐
    │                     │                  │
    ▼                     ▼                  ▼
Set Filters      Click "Apply Filters"   Click "Export"
    │                     │                  │
    └─────────┬───────────┘                  │
              │                              │
              ▼                              ▼
    ┌──────────────────┐           ┌────────────────┐
    │ useInvoiceReports│           │ showExportModal│
    │ useOrderReports  │           │ = true         │
    └──────────────────┘           └────────────────┘
              │                              │
              ▼                              ▼
    ┌──────────────────┐           ┌────────────────┐
    │ Filter applied,  │           │ ExportModal    │
    │ data refreshed   │           │ component      │
    │ on screen        │           │ renders        │
    └──────────────────┘           └────────────────┘
              │                              │
              │                    ┌─────────┴─────────┐
              │                    │                   │
              │                    ▼                   ▼
              │            CSV Export        PDF Export
              │            (User clicks)     (User clicks)
              │                    │                   │
              │                    ▼                   ▼
              │            handleCSVExport   handlePDFExport
              │            setIsExporting    setIsExporting
              │                    │                   │
              │                    ▼                   ▼
              │            exportReportToCSV exportReportToPDF
              │            (or exportToCSV)  (+ landscape layout)
              │                    │                   │
              │                    ▼                   ▼
              │            Data formatting   jsPDF + html2canvas
              │                    │                   │
              │                    ▼                   ▼
              │            CSV Blob created   PNG rendered
              │                    │                   │
              │                    ▼                   ▼
              │            Download triggered Download triggered
              │                    │                   │
              └────────┬───────────┴───────────┬───────┘
                       │                       │
                       ▼                       ▼
            [Data updated on screen] [File downloaded]
```

---

## Component Dependencies

```
ReportsModule
    │
    ├── useInvoiceReports (hook)
    ├── useOrderReports (hook)
    │
    ├── ExportModal (NEW)
    │   └── UI: Modal, buttons, icons
    │
    ├── SummaryCard
    ├── ReportCard
    └── InvoiceDetailsTable
        │
        └── (Uses calculateCreditPeriod function)

useReports.ts
    │
    ├── exportToCSV (existing)
    │   └── For invoice details export
    │
    ├── exportReportToCSV (NEW)
    │   └── For summary/report data as CSV
    │
    ├── exportInvoiceToPDF (existing + enhanced)
    │   └── For invoice details as PDF
    │
    └── exportReportToPDF (NEW)
        └── For summary/report data as PDF
            ├── Landscape orientation
            ├── Full borders
            ├── Page breaks
            └── Professional styling
```

---

## State Management

```
ReportsModule State:
├── reportType: 'invoices' | 'orders'
├── groupBy: 'all' | 'buyer' | 'mfg'
├── invoiceFilter: 'all' | 'overdue' | 'dueToday' | 'dueSoon'
├── invoiceStatusFilter: 'all' | 'paid' | 'unpaid'
├── paymentTypeFilter: 'all' | 'cash' | 'gst'
├── orderStatus: 'all' | 'pending' | 'completed'
├── selectedBuyer: string
├── selectedMfg: string
├── expandedItem: string | null
├── invoiceView: 'summary' | 'details'
├── showExportModal: boolean (NEW)
│   └── Controls ExportModal visibility
├── isExporting: boolean (NEW)
│   └── Loading state during export
└── [derived from hooks]
    ├── data: Report data
    ├── isLoading: boolean
    └── error: string | null
```

---

## Filter to Export Flow

```
┌────────────────────────────────────────────────────────────┐
│ STEP 1: USER SELECTS FILTERS                              │
│                                                            │
│ User sets:                                                 │
│ • Due Date: "Overdue"                                     │
│ • Status: "UNPAID"                                        │
│ • Payment Type: "Cash"                                    │
│ • Buyer: "ABC Textiles"                                  │
└────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────┐
│ STEP 2: USER CLICKS "APPLY FILTERS"                       │
│                                                            │
│ handleApplyFilters() called                               │
│ • Filter state is already set from Step 1                 │
│ • useInvoiceReports hook re-runs with new params         │
│ • Data is fetched from Firebase with filters applied      │
│ • Component re-renders with filtered data                 │
│ • NO download triggered (different from old behavior)    │
└────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────┐
│ STEP 3: FILTERED DATA DISPLAYED                           │
│                                                            │
│ Screen shows:                                              │
│ • Summary cards updated with filtered counts              │
│ • Detailed report shows only matching records             │
│ • Example: 5 overdue, unpaid cash invoices from ABC       │
│                                                            │
│ Export button is now ready with this filtered data        │
└────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────┐
│ STEP 4: USER CLICKS "EXPORT / DOWNLOAD"                   │
│                                                            │
│ setShowExportModal(true)                                  │
│ ExportModal component renders with two options            │
└────────────────────────────────────────────────────────────┘
                         │
              ┌──────────┴──────────┐
              │                     │
              ▼                     ▼
    ┌─────────────────┐   ┌─────────────────┐
    │ User clicks CSV │   │ User clicks PDF │
    └─────────────────┘   └─────────────────┘
              │                     │
              ▼                     ▼
    ┌─────────────────┐   ┌─────────────────┐
    │ handleCSVExport │   │ handlePDFExport │
    └─────────────────┘   └─────────────────┘
              │                     │
              ▼                     ▼
    ┌──────────────────────────────────────┐
    │ Both functions:                      │
    │ 1. setIsExporting(true)              │
    │ 2. Format data respecting filters    │
    │ 3. Generate file                     │
    │ 4. Trigger download                  │
    │ 5. setShowExportModal(false)         │
    │ 6. setIsExporting(false)             │
    └──────────────────────────────────────┘
              │
              ▼
    ┌──────────────────────────┐
    │ File Downloaded          │
    │ • invoice-details_      │
    │   2025-12-31.csv        │
    │ (or .pdf)               │
    │                          │
    │ Contains ONLY:           │
    │ • Filtered data          │
    │ • 5 records matching     │
    │   the applied filters    │
    └──────────────────────────┘
```

---

## PDF Generation Pipeline

```
USER CLICKS "Download as PDF"
    │
    ▼
setIsExporting(true)
setShowExportModal(true) = false (close modal)
    │
    ▼
handlePDFExport() async function
    │
    ├─ Collect filtered data from state
    │
    ├─ Format data:
    │  ├─ Currency formatting
    │  ├─ Date formatting
    │  └─ Field mapping
    │
    ├─ Call exportReportToPDF()
    │  │
    │  ├─ jsPDF library imported
    │  │  └─ Create landscape PDF (A4)
    │  │
    │  ├─ Calculate pages needed
    │  │  └─ 25 rows per page
    │  │
    │  ├─ Loop through pages
    │  │  │
    │  │  ├─ Create HTML container (off-screen)
    │  │  │
    │  │  ├─ Build HTML table with styling:
    │  │  │  ├─ Dark header (#1f2937)
    │  │  │  ├─ Full borders (1px solid)
    │  │  │  ├─ Alternating row colors
    │  │  │  ├─ Proper padding
    │  │  │  ├─ Currency formatting
    │  │  │  └─ Page number footer
    │  │  │
    │  │  ├─ html2canvas renders HTML to image
    │  │  │  └─ Canvas with scale: 2 for quality
    │  │  │
    │  │  ├─ Convert canvas to PNG
    │  │  │
    │  │  ├─ Add image to PDF page
    │  │  │  └─ With proper positioning and sizing
    │  │  │
    │  │  └─ Clean up (remove HTML container)
    │  │
    │  └─ Save PDF file
    │     └─ Filename: report_YYYY-MM-DD.pdf
    │
    ├─ Close modal
    │
    └─ setIsExporting(false)
        │
        ▼
    File downloads to user's Downloads folder
```

---

## Browser Download Flow

```
┌─────────────────────────────────────┐
│ JavaScript generates file blob      │
│ (CSV or PDF in memory)              │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ Create object URL                   │
│ const url = URL.createObjectURL()   │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ Create hidden link element          │
│ <a href="blob:..." download="..."/> │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ Programmatically click link         │
│ link.click()                        │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ Browser triggers download           │
│ File appears in Downloads folder    │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ Clean up resources                  │
│ • Remove link from DOM              │
│ • Revoke object URL                 │
└─────────────────────────────────────┘
```

---

## PDF Page Layout (Landscape A4)

```
┌─────────────────────────────────────────────────────────┐
│                     297mm (Width)                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Title: "Invoice Report"                (15mm pad)│  │
│  │ Date: "Generated on: 2025-12-31"                 │  │
│  ├───────────────────────────────────────────────────┤  │
│  │ ┌─────┬──────────┬──────────┬─────────────────┐  │  │
│  │ │ SR  │ Invoice# │ Date     │ ... More Cols │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │  1  │ INV-001  │ 31-12-25 │ ...           │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │  2  │ INV-002  │ 30-12-25 │ ...           │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │ ... │ ...      │ ...      │ ...           │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │ 25  │ INV-025  │ 07-12-25 │ ...           │  │  │
│  │ └─────┴──────────┴──────────┴─────────────────┘  │  │
│  │                  Page 1 of 3                     │  │
│  └───────────────────────────────────────────────────┘  │
│                     210mm (Height)                      │
└─────────────────────────────────────────────────────────┘

                    Next Page (Page 2)
                    ↓
┌─────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────┐  │
│  │ ┌─────┬──────────┬──────────┬─────────────────┐  │  │
│  │ │ SR  │ Invoice# │ Date     │ ... More Cols │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │ 26  │ INV-026  │ 06-12-25 │ ...           │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │ 27  │ INV-027  │ 05-12-25 │ ...           │  │  │
│  │ ├─────┼──────────┼──────────┼─────────────────┤  │  │
│  │ │ ... │ ...      │ ...      │ ...           │  │  │
│  │ └─────┴──────────┴──────────┴─────────────────┘  │  │
│  │                  Page 2 of 3                     │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

Key Features:
✓ Landscape orientation (wider)
✓ 15mm padding on all sides
✓ Dark header row with borders
✓ All cells have visible borders
✓ 25 rows per page (optimized)
✓ Page numbers on every page
✓ Alternating row colors
✓ Professional appearance
```

---

## File Size Estimates

```
Data Volume → Export Size

100 invoices:
  CSV:  ~15 KB
  PDF:  ~200 KB (single page)

500 invoices:
  CSV:  ~75 KB
  PDF:  ~800 KB (2-3 pages)

1000 invoices:
  CSV:  ~150 KB
  PDF:  ~1.5 MB (4-5 pages)

Notes:
• CSV is much smaller
• PDF sizes depend on content and styling
• Both are suitable for email/sharing
• No compression used (uses browser defaults)
```

---

## Error Handling

```
Try
├─ setIsExporting(true)
├─ Validate data exists
├─ Format data
├─ Call export function
│  ├─ jsPDF library import
│  ├─ html2canvas library import
│  ├─ Generate file
│  └─ Trigger download
├─ setShowExportModal(false)
└─ setIsExporting(false)

Catch
├─ Log error to console
├─ Keep modal open
├─ Keep loading state active
└─ User can retry

Finally
└─ setIsExporting(false) (ensures clean state)
```

---

**All diagrams designed for clarity and testing purposes**
